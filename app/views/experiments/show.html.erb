<h1><%= @experiment.name %></h1>
<p>
  Download:
  <%= link_to "CSV", :action => :show, :id => @experiment.id, :format =>'csv'%>

</p>

<br>
 <% if @experiment.sensor_data.empty? %>
 <p>This experiment has no data yet.

 <% if @experiment.active? %>
 <% elsif @experiment.start.nil? %>
 Press Start Experiment to begin collecting data.
 <% end %>
 </p>
 <% else %>
  <%= render 'chart' %>
 <% end %>

<br>

<p>
	<%= render "tooltip", tooltip: "The building and room where the experiment takes place.", visible: "<strong>Experiment Location:</strong>" %>
  <%= @experiment.location %>
</p>

<p>
	  <%= render "tooltip", tooltip: "Sensors stop collecting data when the CO2 levels fall below this amount.", visible: "<strong>CO2 Cutoff: </strong>" %>
  <%= @experiment.co2_cutoff %> ppm
</p>

<%= render 'show_start_and_end' %>

<p>
	<strong>Details:</strong> <br> &nbsp;&nbsp;&nbsp;&nbsp;
	<%= @experiment.details %>
</p>

<%= render 'show_devices' %>
<br>
<%= link_to 'Back', experiments_path %> | 
<%= link_to 'Edit', edit_experiment_path(@experiment) %>